import shop from '../../api/api_pvs'
//import nested from './nested'

// initial state
const state = () => ({
  test:"hhhh"
})

// getters
const getters = {
  
  }


// actions
const actions = {
  
}

// mutations
const mutations = {
  
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}